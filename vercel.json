{
  "installCommand": "if [ -d apps/dashboard ]; then cd apps/dashboard && npm install; else npm install; fi",
  "buildCommand": "if [ -d apps/dashboard ]; then cd apps/dashboard && npm run build; else npm run build; fi",
  "framework": "nextjs"
}
